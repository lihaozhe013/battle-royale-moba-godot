# Linux 配置模板
# 复制为 build_env.yaml 后填写

toolchain: gcc

cmake_bin_dir: ""  # 留空 = 从 PATH 自动检测

python3_executable: ""  # 留空 = 用 .venv或系统 python3

build:
  target: template_debug
  jobs: 0
  verbose: false
